# Copy to .env and fill in values before starting the stack.
# The .env file is gitignored.

# Grafana admin password
GRAFANA_PASSWORD=changeme

# Hostname/IP of this machine as seen from remote hosts (for Alloy → Loki)
# This is used in agent configs to know where to ship logs.
LOKI_HOST=openclaw
